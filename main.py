from gui.main_gui import App

if __name__ == "__main__":
    """
    Entry point to launch the GUI application.
    Creates an instance of App and starts its main event loop.
    """
    app = App()
    app.mainloop()
