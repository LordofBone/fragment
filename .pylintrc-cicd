[MASTER]
# A comma-separated list of file name patterns to be excluded from checks.
ignore-patterns=tests/*,migrations/*

[MESSAGES CONTROL]
# Disable the message, report, category or checker with the given id(s).
disable=
    C0114,  # Missing module docstring
    C0115,  # Missing class docstring
    C0116,  # Missing function or method docstring
    W0401,  # Wildcard import
    W0201,  # Attribute defined outside __init__
    E0602,  # Undefined variable
    W0611,  # Unused import
    W0612,  # Unused variable
    R0801,  # Duplicate code
    R0902,  # Too many instance attributes
    R0913,  # Too many arguments
    R0914,  # Too many local variables
    E0401,  # Import errors
    E1101,  # No member
    R0201,  # Method could be a function

# Enable the following messages (focused on common issues that are often fixed manually)
enable=
    C0103,  # Variable name doesn't conform to snake_case naming style
    C0301,  # Line too long
    C0302,  # Too many lines in module
    C0303,  # Trailing whitespace
    C0304,  # Final newline missing
    C0321,  # Multiple statements on one line (colon)

[FORMAT]
# Maximum number of characters on a single line.
max-line-length=120

[BASIC]
# Variable names that should be ignored from the snake_case convention
good-names=i,j,k,ex,Run,_,env,GL,glm,gl

[REPORTS]
# Tells whether to display a full report or only the messages
reports=no
